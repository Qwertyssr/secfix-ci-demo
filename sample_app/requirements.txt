# Application dependencies (intentionally old/vulnerable for the demo).
# Black Duck (SCA) reports the CVEs; the agent bumps these to the fixed versions.
PyYAML==5.4
requests==2.20.0
Jinja2==2.10.1
urllib3==1.24.1
