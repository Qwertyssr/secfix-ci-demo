"""secfix — a CI/CD security auto-fix agent.

Reads normalized findings from security scanners (Black Duck SCA, Fortify SAST),
generates fixes, validates them, and opens a pull request.
"""
__version__ = "0.1.0"
